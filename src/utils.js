// src/utils.js: Example utility module.
/**
 * Returns a greeting message.
 *
 * @param {string} name - Person to greet
 * @returns {string}
 */
function greet(name) {
  return `👋 Hello, ${name}!`;
}

module.exports = { greet };
